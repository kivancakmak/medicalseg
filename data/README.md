# Data Directory

Use this folder for local datasets and generated samples.

Suggested layout:

```text
data/
  cholecseg/
    images/
    masks/
  synthetic/
    generated/
```

The current prototype can run without any downloaded data by generating synthetic scenes on the fly.

